# LacEditor 下一轮优化清单

本清单记录本轮稳定性工作刻意未扩大的范围。下一次优化从第 1 项继续，完成后同步更新
`CHANGELOG.md`、`ACCEPTANCE_TESTS.md` 和本文件。

1. 评估并引入成熟 CommonMark/GFM 解析器，补齐松散列表、`1)`、复杂续行和表格转义。
2. 扩展 XCTest 到真实窗口、拖拽、编辑器缓存和内存压力，并拆分过大的 SwiftUI body 与核心文件。
3. 增加需确认后执行的构建缓存清理脚本。
4. 经用户确认后清理可再生成的构建产物；不触碰 `Design/IconConcept/` 图标草稿。

侧边栏后续可选增强：支持键盘导航与多选批量管理；
第一版不增加颜色标签、智能分组、文件内容索引或磁盘文件夹同步。

本轮仍保留的实现限制：Foundation 的 JSON 解析和部分字符串转换无法在单次系统调用内部硬取消；
当前已通过任务代次与结果校验阻止过期结果写回，下一轮可继续细化任务合并和内存预算。
