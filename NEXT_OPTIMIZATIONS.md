# LacEditor 下一轮优化清单

本清单记录当前仍未进入实现的后续范围。下一次优化从第 1 项继续，完成后同步更新
`CHANGELOG.md`、`ACCEPTANCE_TESTS.md` 和本文件。

1. 为侧边栏增加键盘导航与多选批量管理。
2. 继续细化后台任务合并和峰值内存预算，重点观察多个 50 MB 标签同时恢复时的内存曲线。

暂不增加颜色标签、智能分组、文件内容索引或磁盘文件夹同步。

本轮仍保留的实现限制：Foundation 的 JSON 解析和部分字符串转换无法在单次系统调用内部硬取消；
当前已通过任务代次与结果校验阻止过期结果写回。
