# LacEditor 版本管理规范

## 目标

这套规则同时管理三件事：

1. Git 历史清楚，可以定位每项变化来自哪里。
2. App 版本号、Xcode 构建号和 Git 标签保持一致。
3. 每个版本都有面向用户的中文更新说明，明确区分新增、优化和修复。

## 版本号

正式版本使用 `主版本.次版本.修订版本`，例如 `1.4.2`：

- 主版本：存在不兼容变化或产品进入新的稳定阶段，例如 `1.0.0` 到 `2.0.0`。
- 次版本：加入向后兼容的新功能，例如 `0.1.0` 到 `0.2.0`。
- 修订版本：只包含兼容的错误修复或小幅体验优化，例如 `0.1.0` 到 `0.1.1`。

版本号必须同时出现在：

- 根目录的 `VERSION`。
- Xcode 的 `MARKETING_VERSION`。
- `CHANGELOG.md` 的版本标题。
- Git 标签 `v<版本号>`，例如 `v0.2.0`。

`CURRENT_PROJECT_VERSION` 是递增的内部构建号。每次正式发布至少增加 1，不因版本号降级或重置。

## 分支

- `main`：始终保持可构建状态，只保留已经验证的工作。
- `feature/<名称>`：开发新功能。
- `fix/<名称>`：普通错误修复。
- `hotfix/<版本>`：从已发布版本紧急修复。
- `release/<版本>`：需要多次测试或候选构建时使用。
- `codex/<名称>`：由 Codex 创建的工作分支。

分支应保持短期存在，完成验证后合并回 `main`，不在长期分支上堆积多个无关功能。

## 提交信息

提交使用 Conventional Commits 前缀，正文可以使用自然、准确的中文：

```text
feat: 支持 JavaScript 文件和语法高亮
fix: 修复 Markdown 嵌套列表编号重置
perf: 限制大文件语法高亮范围
refactor: 拆分文件读写与标签状态
docs: 补充版本发布规范
test: 增加查找替换核心验证
build: 接入 Apple Generic Versioning
chore(release): v0.2.0
```

一个提交只处理一个主题。不要使用“更新代码”“修复问题”等无法说明影响的描述。

## 更新日志

每个影响用户的分支都必须同步更新 `CHANGELOG.md` 的“未发布”部分：

- 新增：新文件格式、新命令、新界面能力或新工作流。
- 优化：性能、交互、布局、可读性或现有行为的改善。
- 修复：原有行为不正确、崩溃、显示异常或数据错误。
- 变更：会改变既有使用方式，但不一定属于新增或修复。
- 安全：权限、隐私、数据保护或依赖安全变化。

写作要求：

- 描述用户能获得什么结果，不写内部类名或只罗列文件。
- 一条只表达一个变化，必要时说明触发场景。
- 修复应写清“什么情况下发生了什么错误”。
- 不把重构、格式化、测试补充包装成用户功能。
- 同一项变化不要同时出现在多个分类。

## 发布流程

1. 确认目标版本，并把“未发布”中的实际内容移动到
   `## [版本号] - YYYY-MM-DD` 下。
2. 在“未发布”下恢复空的新增、优化、修复分类，供下一轮开发继续记录。
3. 执行发布准备脚本：

   ```bash
   ./Scripts/prepare-release.sh 0.2.0
   ```

   脚本会更新 `VERSION` 和 Xcode 营销版本、增加构建号，并执行完整发布验证。
4. 人工阅读该版本的更新说明，确认内容完整、无内部术语、日期正确。
5. 提交并创建带说明的标签：

   ```bash
   git add .
   git commit -m "chore(release): v0.2.0"
   git tag -a v0.2.0 -m "LacEditor v0.2.0"
   ```

6. 如果配置了远程仓库，再推送分支和标签：

   ```bash
   git push origin main
   git push origin v0.2.0
   ```

7. 创建 GitHub/GitLab Release 时，直接使用 `CHANGELOG.md` 中对应版本的新增、优化和修复内容。

## 发布检查

发布脚本会执行以下检查：

- `VERSION`、Xcode 营销版本和 `CHANGELOG.md` 版本一致。
- Xcode 工程文件格式有效。
- SwiftPM Debug 与 Release 构建成功。
- JSON、Markdown、查找替换和折叠核心验证通过。
- Xcode Release 构建成功。

涉及界面的版本还需要人工检查：

- 浅色和深色模式。
- 主窗口缩放与 Markdown 分栏拖动。
- 打开、编辑和保存支持的文件格式。
- 未保存确认、菜单和快捷键。
- 更新日志中提到的用户场景。

正式候选版本还必须执行 `./Scripts/run-acceptance.sh`，并按照
`ACCEPTANCE_TESTS.md` 完成人工 UI 用例。验收结果保存到 `AcceptanceReports/`；P0 或 P1
用例失败时不得创建版本标签。

## 紧急修复

紧急修复从已发布标签创建分支，例如：

```bash
git switch -c hotfix/0.1.1 v0.1.0
```

只提交修复和必要验证，版本号增加修订位，更新日志写入“修复”，完成后同时合并回 `main`。
