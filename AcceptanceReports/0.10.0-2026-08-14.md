# LacEditor 0.10.0 验收报告

## 基本信息

- 验收日期：2026-08-14
- 验收对象：LacEditor `0.10.0 (10)`
- 系统：macOS 26.6，Apple Silicon
- 最低目标系统：macOS 26.0
- 架构：arm64
- 验收标准：`ACCEPTANCE_TESTS.md`

## 版本范围

- 新增正常退出工作区恢复，保留多窗口、标签顺序、正文、未保存状态、选区、滚动位置和界面状态。
- 设置新增退出行为，可选择保留工作区或退出前逐文档检查未保存内容。
- JSON 格式化与压缩改为保序扫描，保留字段顺序、重复键、数值和字符串转义写法。
- 默认额外行距调整为 4 pt，并支持在设置中调整和恢复默认。
- 加深浅色与深色模式下的当前行背景，提高代码阅读辨识度。

## 自动化结果

执行命令：

```bash
git diff --check
./Scripts/prepare-release.sh 0.10.0
./Scripts/run-acceptance.sh
```

结果：**全部通过**。

| 范围 | 结果 | 证据摘要 |
| --- | --- | --- |
| XCTest | 通过 | 32 项测试通过，覆盖工作区、异常恢复、JSON 保序、窗口、缓存、编码和稳定性 |
| SwiftPM Debug/Release | 通过 | 两种配置均成功构建 |
| Xcode Release/Analyze | 通过 | macOS 26、arm64 Release 与静态分析完成 |
| 工作区恢复 | 通过 | 两窗口、多标签、标签顺序、正文及未保存状态往返测试通过 |
| JSON 工具 | 通过 | 字段顺序、重复键、原始数值与字符串转义保持，取消检查通过 |
| 编辑器布局 | 通过 | 默认及自定义行距、自动换行、折叠和行号对齐验证通过 |
| 文件与性能 | 通过 | 27 种格式、4 MiB 往返及 1/10/20/50/100 MB 门禁通过 |
| 产物与隐私 | 通过 | 版本、架构、图标、最低系统和无网络权限检查通过 |

本机关键数据：10 MB 深位置布局最慢 `0.365s`，连续宽度变化与布局 `0.019s`；
10 MB 首次高亮 `0.223s`、缓存命中 `0.012s`；50 MB JSON 解码 `0.163s`，
100 MB 文本解码 `0.342s`。

## 人工检查

- 用户已确认当前候选版本测试无问题，可以正式发布。
- 已检查正常退出工作区恢复、传统未保存确认、JSON 格式化、行距、当前行和 Markdown 预览。
- 未发现 P0 或 P1 阻断项。

## 已知限制

- 50 MB 是正式性能目标，100 MB 继续采用尽力支持策略。
- 包含多个大型标签的工作区当前会在启动时完整读取；后续将改为当前标签优先的渐进恢复。
- 当前发布使用本机 ad-hoc 签名，未执行 Apple Developer ID 签名、公证或 App Store 分发。
