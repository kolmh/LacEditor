# LacEditor 验收测试标准

本文是 LacEditor 每次迭代和正式发布共用的验收基线。验收结论必须来自当前提交的自动化结果和真实 macOS UI 操作，不能只以“可以构建”代替功能验收。

## 1. 适用范围与通过门槛

- **P0**：崩溃、卡死、数据丢失、无法打开或保存文件、错误覆盖文件。任何一项失败都禁止发布。
- **P1**：第一阶段核心功能、已发布功能、主要快捷键、深浅色可读性。必须全部通过。
- **P2**：轻微动画、边距或非阻断视觉问题。可以带已知限制通过，但必须写入验收报告。
- 自动化门禁必须完整通过；人工用例必须在浅色、深色和跟随系统三种外观下抽查。
- 窗口至少验证 `900 x 560`、`1120 x 720` 和全屏三种尺寸，无重叠、裁切或不可达控件。
- 验收环境、提交号、版本号、失败证据、修复和回归结果必须写入 `AcceptanceReports/`。

## 2. 数据隔离

自动化验收执行：

```bash
./Scripts/run-acceptance.sh
```

脚本构建 Bundle ID 为 `com.laceditor.acceptance` 的独立 App，UI 验收应使用该产物：

```text
.build/AcceptanceDerivedData/Build/Products/Release/LacEditor.app
```

这样不会覆盖正式 LacEditor 的最近文件、侧边栏和主题偏好。测试文件只能放在临时目录或 `Verification/Fixtures/`，不得覆盖用户文件。

## 3. 自动化用例

| ID | 优先级 | 验收内容 | 通过标准 |
| --- | --- | --- | --- |
| AUTO-BUILD-01 | P0 | SwiftPM Debug/Release | 均成功，无编译错误 |
| AUTO-BUILD-02 | P0 | Xcode Release | macOS 14 目标构建成功 |
| AUTO-BUILD-03 | P0 | 架构 | App 同时包含 arm64、x86_64 |
| AUTO-BUILD-04 | P1 | 版本一致性 | `VERSION`、Xcode、Info.plist、CHANGELOG 一致 |
| AUTO-BUILD-05 | P1 | App 图标 | 16 至 1024 像素资源完整 |
| AUTO-BUILD-06 | P1 | Xcode 静态分析 | Release Analyze 完成且无分析器错误 |
| AUTO-FILE-01 | P0 | UTF-8 文件往返 | 所有支持扩展内容逐字节等价 |
| AUTO-FILE-02 | P0 | 重命名与路径迁移 | 不覆盖同名目标，非法文件名被拒绝；路径扩展变化后语言与预览同步 |
| AUTO-FILE-03 | P1 | 文件树过滤 | 隐藏和不支持文件不进入树 |
| AUTO-FILE-04 | P1 | 4 MiB 文件往返 | 原子保存后按 UTF-8 读回，内容逐字节等价 |
| AUTO-JSON-01 | P1 | JSON 格式化/压缩 | 输出合法、稳定并符合预期换行 |
| AUTO-JSON-02 | P1 | JSON 错误 | 无效 JSON 被拒绝并给出明确中文信息 |
| AUTO-MD-01 | P1 | Markdown 块语法 | 标题、引用、列表、分割线、代码块、表格正确 |
| AUTO-MD-02 | P1 | Markdown 行内语法 | 强调、链接、行内代码互不错误嵌套 |
| AUTO-EDIT-01 | P1 | 查找替换 | 前后查找、循环、大小写和全部替换正确 |
| AUTO-EDIT-02 | P1 | 转义字符 | `\\n`、`\\r`、`\\t`、`\\s`、`\\\\` 正确解释 |
| AUTO-EDIT-03 | P1 | Markdown 列表 | 续号、插入删除重排、粘贴显式编号正确 |
| AUTO-HL-01 | P1 | 语言识别 | 所有支持扩展映射到正确语言 |
| AUTO-HL-02 | P1 | 高亮边界 | 字符串、正则和注释不能污染后续代码 |
| AUTO-FOLD-01 | P1 | 折叠 | Markdown 标题和 JSON 嵌套容器范围正确 |
| AUTO-FOLD-02 | P1 | 折叠布局 | 折叠后隐藏行高度收缩，展开恢复且编辑不残留旧状态 |
| AUTO-PERF-01 | P1 | 4 MB 行索引 | 当前基准机器 5 秒内完成 |
| AUTO-PERF-02 | P1 | 4 MB 查找 | 当前基准机器 3 秒内完成 |
| AUTO-PERF-03 | P1 | 可视区高亮 | 当前基准机器 3 秒内完成 |
| AUTO-PRIV-01 | P0 | 权限与网络 | 无网络 entitlement、URLSession 或 Network API |

性能阈值是防止数量级回退的宽松门槛，不用于产品间跑分。基准机器或 SDK 变化时，应同时记录旧值和新值后再调整。

## 4. 人工功能用例

### 文件与编码

| ID | 操作 | 预期结果 |
| --- | --- | --- |
| UI-FILE-01 | 新建内容，保存，再关闭重开 | 内容完整，标题和最近文件更新，状态为“已保存” |
| UI-FILE-02 | 执行“另存为”并选择不同扩展 | 原文件不被覆盖，新标签路径、语言和图标更新 |
| UI-FILE-03 | 同一窗口重复打开同一文件 | 定位已有标签，不创建副本 |
| UI-FILE-04 | 拖入 TXT、MD、HTML、JSON、JS | 文件正常打开，语言模式正确 |
| UI-FILE-05 | 打开非 UTF-8 文件 | 显示编码选择；取消不创建标签，正确编码可打开 |
| UI-FILE-06 | 修改后关闭标签、窗口和退出 | 每种路径均出现保存/不保存/取消确认 |
| UI-FILE-07 | 清空新建文档后关闭 | 不提示保存；唯一空白标签再次关闭时关闭窗口 |
| UI-FILE-08 | 最近文件右键菜单 | 打开、新窗口打开、重命名、Finder 定位、移除均正确 |

### 标签与窗口

| ID | 操作 | 预期结果 |
| --- | --- | --- |
| UI-TAB-01 | 单标签和双标签之间切换 | 单标签隐藏标签栏，双标签显示，布局平滑 |
| UI-TAB-02 | 使用 `⌘1...9`、`⌃Tab`、`⌥⌘←/→` | 切换到正确标签，无明显延迟 |
| UI-TAB-03 | 拖动标签到首位、末位和中间 | 顺序正确，末尾剩余区域均可作为落点 |
| UI-TAB-04 | 创建 10 个以上标签并滚动标签栏 | 滚轮横向移动列表，边缘渐隐准确，不切换标签 |
| UI-TAB-05 | 将标签拖出窗口再拖回 | 新窗口创建且应用不卡死，内容和未保存状态保留 |
| UI-TAB-06 | `⌘N` | 新建独立窗口，侧边栏按钮、菜单和主题均可用 |
| UI-TAB-07 | 标签右键关闭其他/右侧 | 目标集合正确，未保存文档逐一确认 |

### 编辑、查找与工具

| ID | 操作 | 预期结果 |
| --- | --- | --- |
| UI-EDIT-01 | 输入、删除、粘贴后撤销/重做 | 内容、光标和未保存状态正确恢复 |
| UI-EDIT-02 | 全部替换后撤销 | 一次 `⌘Z` 恢复替换前全文 |
| UI-EDIT-03 | JSON 格式化/压缩后撤销 | 一次 `⌘Z` 恢复操作前全文 |
| UI-EDIT-04 | `⌘F` 打开查找，再点击正文输入 | 查找为非模态独立窗口，正文仍可编辑 |
| UI-EDIT-05 | `⌥⌘F` 原地切换替换模式，使用转义字符 | 同一面板切换，全部替换数量和结果正确 |
| UI-EDIT-06 | 自动换行开关与分栏拖动 | 开启后无水平漂移，文字不进入预览区域 |
| UI-EDIT-07 | 显示/隐藏行号和状态栏 | 布局立即更新，行号与逻辑行一致 |
| UI-EDIT-08 | `⌘+`、`⌘-`、`⌘0` | 字体在 9–32 pt 范围变化并可恢复 |
| UI-EDIT-09 | Markdown 有序列表回车、插入、删除、粘贴 | 自动续号但不重写粘贴的显式编号 |
| UI-EDIT-10 | Markdown/JSON 折叠命令 | 当前区块折叠和展开，编辑后安全展开 |
| UI-EDIT-11 | 大文件中保留离屏光标，滚动后提交普通输入和中文输入法文本 | 回到真实编辑行；光标、状态栏和行号一致，不跳到文件末尾 |

### 预览、高亮与状态

| ID | 操作 | 预期结果 |
| --- | --- | --- |
| UI-MD-01 | 编辑含标题、列表、引用、链接、代码、表格的 MD | 预览约 180 ms 后更新为最新内容，无旧序号残留 |
| UI-MD-02 | 快速连续输入 20 次 | 最终预览与编辑器最后内容一致 |
| UI-MD-03 | 拖动预览分隔线并缩到最小窗口 | 两侧不重叠、不遮挡，均保持最小可用宽度 |
| UI-HL-01 | 打开每种代码格式 | 语言名称、图标和基础语法颜色正确 |
| UI-HL-02 | 在字符串中输入注释符，在注释后换行 | 字符串不变注释色，下一行恢复正常高亮 |
| UI-JSON-01 | 格式化无效 JSON | 内容不改写，状态栏显示错误并尽量包含行列 |
| UI-STATUS-01 | 移动光标和编辑中英文 | 保存状态、UTF-8、语言、字数、行列实时正确 |

### 外观、菜单与稳定性

| ID | 操作 | 预期结果 |
| --- | --- | --- |
| UI-VIEW-01 | 浅色、深色、跟随系统依次切换 | 背景、文字、预览和设置均可读，无残留错误颜色 |
| UI-VIEW-02 | 展开/收起侧边栏并重启验收 App | 状态被记住，标题和第一行行号不跳动 |
| UI-VIEW-03 | 收起侧边栏后悬停按钮 | 抽屉临时出现，移开后稳定消失 |
| UI-VIEW-04 | 展开“显示 > 外观”等二级菜单 | 子菜单可通过鼠标悬停打开，图标与文字对齐 |
| UI-VIEW-05 | 悬停所有工具按钮至少 2 秒 | Tooltip 稳定显示；有快捷键时同时显示快捷键 |
| UI-VIEW-06 | 打开设置窗口 | 540 × 340 内容完整，无裁切，三种主题下均可读 |
| UI-STAB-01 | 打开并编辑 4 MB 文件 2 分钟 | 输入和滚动可用，无卡死或异常内存增长 |
| UI-STAB-02 | 连续切换 10 个标签 50 次 | 无崩溃、内容串页、明显逐次变慢 |

## 5. 回归与报告规则

发现缺陷后必须：记录失败用例 ID和复现步骤；修复源码；为可自动化的问题增加回归测试；重新执行失败用例和完整自动门禁。验收报告应区分“首次通过”“修复后通过”“已知限制”，不得删除失败历史来制造全绿结果。
