# LacEditor 验收测试标准

本文是 LacEditor 每次迭代和正式发布共用的验收基线。验收结论必须来自当前提交的自动化结果和真实 macOS UI 操作，不能只以“可以构建”代替功能验收。

## 1. 适用范围与通过门槛

- **P0**：崩溃、卡死、数据丢失、无法打开或保存文件、错误覆盖文件。任何一项失败都禁止发布。
- **P1**：第一阶段核心功能、已发布功能、主要快捷键、深浅色可读性。必须全部通过。
- **P2**：轻微动画、边距或非阻断视觉问题。可以带已知限制通过，但必须写入验收报告。
- 自动化门禁必须完整通过；人工用例必须在浅色、深色和跟随系统三种外观下抽查。
- 窗口至少验证 `900 x 560`、`1120 x 720` 和全屏三种尺寸，无重叠、裁切或不可达控件。
- 验收环境、提交号、版本号、失败证据、修复和回归结果必须写入 `AcceptanceReports/`。

## 2. 数据隔离

自动化验收执行：

```bash
./Scripts/run-acceptance.sh
```

脚本构建 Bundle ID 为 `com.laceditor.acceptance` 的独立 App，UI 验收应使用该产物：

```text
.build/AcceptanceDerivedData/Build/Products/Release/LacEditor.app
```

这样不会覆盖正式 LacEditor 的最近文件、侧边栏和主题偏好。测试文件只能放在临时目录或 `Verification/Fixtures/`，不得覆盖用户文件。

## 3. 自动化用例

| ID | 优先级 | 验收内容 | 通过标准 |
| --- | --- | --- | --- |
| AUTO-BUILD-01 | P0 | SwiftPM Debug/Release | 均成功，无编译错误 |
| AUTO-BUILD-02 | P0 | Xcode Release | macOS 26 目标构建成功 |
| AUTO-BUILD-03 | P0 | 架构 | App 为 Apple Silicon arm64 |
| AUTO-BUILD-04 | P1 | 版本一致性 | `VERSION`、Xcode、Info.plist、CHANGELOG 一致 |
| AUTO-BUILD-05 | P1 | App 图标 | 16 至 1024 像素资源完整 |
| AUTO-BUILD-06 | P1 | Xcode 静态分析 | Release Analyze 完成且无分析器错误 |
| AUTO-MD-01 | P0 | CommonMark/GFM | 松散列表、`1)`、复杂续行、表格转义、任务列表、删除线和自动链接通过 XCTest |
| AUTO-MD-02 | P0 | Markdown 安全 | 原始 HTML 与危险链接协议不会直接进入预览 HTML |
| AUTO-MD-03 | P1 | Markdown 性能 | 约 1 MB Markdown 在测试门槛内完成渲染且输出非空 |
| AUTO-SESSION-01 | P0 | 真实窗口与拖拽 | `NSWindow` 注册、跨窗口拖放及末尾落点通过 XCTest |
| AUTO-SESSION-02 | P0 | 会话缓存 | LRU 数量、64 MB 预算、24 MB 单会话上限及状态恢复通过 XCTest |
| AUTO-SESSION-03 | P0 | 内存压力 | critical 压力清空非活动会话且不淘汰活动编辑器 |
| AUTO-RECOVERY-01 | P0 | 异常会话恢复 | 最新 revision 的正文、路径、编码、磁盘基线、选区和滚动状态可完整恢复 |
| AUTO-RECOVERY-02 | P0 | 正常退出与放弃 | 正常结束会话或关闭文档后不再提供恢复，且不会误删其他文档的恢复副本 |
| AUTO-RECOVERY-03 | P0 | 显式保存边界 | 恢复文档保持未保存状态，刷新状态不会误判为已保存，明确保存后才变为已保存 |
| AUTO-RECOVERY-04 | P1 | 失去焦点 | AppState 在应用失去焦点时立即提交当前未保存内容，不等待普通防抖计时结束 |
| AUTO-CLEAN-01 | P0 | 清理脚本安全 | 脚本语法有效；无确认参数时只预览白名单目标且保留 `.build` |
| AUTO-FILE-01 | P0 | UTF-8 文件往返 | 所有支持扩展内容逐字节等价 |
| AUTO-FILE-02 | P0 | 重命名与路径迁移 | 不覆盖同名目标，非法文件名被拒绝；路径扩展变化后语言与预览同步 |
| AUTO-PREF-01 | P1 | 多窗口共享设置 | 主题、字体、换行、行号和状态栏即时同步，自动换行重启后保留 |
| AUTO-SIDEBAR-01 | P1 | 收藏与分组持久化 | 收藏、分组、折叠和排序写入本地 JSON，重建 Store 后一致 |
| AUTO-SIDEBAR-02 | P1 | 路径迁移与安全删除 | 文件改名后所有快捷入口同步；删除分组不删除磁盘文件 |
| AUTO-FILE-04 | P1 | 4 MiB 文件往返 | 原子保存后按 UTF-8 读回，内容逐字节等价 |
| AUTO-FILE-05 | P0 | 文件身份唯一性 | 原路径与符号链接解析为同一文件身份 |
| AUTO-FILE-06 | P0 | 原编码无损保存 | UTF-16、GB18030 往返一致；不可表示字符时拒绝写入 |
| AUTO-JSON-01 | P1 | JSON 格式化/压缩 | 输出合法、稳定并符合预期换行 |
| AUTO-JSON-02 | P1 | JSON 错误 | 无效 JSON 被拒绝并给出明确中文信息 |
| AUTO-MD-04 | P1 | Markdown 块语法 | 标题、引用、列表、分割线、代码块、表格正确 |
| AUTO-MD-05 | P1 | Markdown 行内语法 | 强调、链接、行内代码互不错误嵌套 |
| AUTO-EDIT-01 | P1 | 查找替换 | 前后查找、循环、大小写和全部替换正确 |
| AUTO-EDIT-02 | P1 | 转义字符 | `\\n`、`\\r`、`\\t`、`\\s`、`\\\\` 正确解释 |
| AUTO-EDIT-03 | P1 | Markdown 列表 | 续号、插入删除重排、粘贴显式编号正确 |
| AUTO-CODEC-01 | P1 | URL 编解码 | UTF-8 组件往返正确；标准模式保留 `+`，表单模式转为空格 |
| AUTO-CODEC-02 | P1 | Base64/Base64URL | 中文往返、URL 安全字母表和无填充格式正确；二进制结果不写入文本 |
| AUTO-CODEC-03 | P1 | HTML/Unicode | 常用及数字 HTML 实体、BMP、Emoji 代理对和 JSON 转义往返正确 |
| AUTO-CODEC-04 | P1 | 智能识别 | URL、Base64、HTML、Unicode 正确识别；普通文本及无效输入不误判 |
| AUTO-CODEC-05 | P0 | 大选区保护 | 8 MB 警告和 32 MB 上限常量正确，URL 编码使用线性缓冲 |
| AUTO-HL-01 | P1 | 语言识别 | 所有支持扩展映射到正确语言 |
| AUTO-HL-02 | P1 | 高亮边界 | 字符串、正则和注释不能污染后续代码 |
| AUTO-HL-03 | P1 | 增量词法状态 | 超过 64 KiB 的块注释和多行字符串正确；编辑闭合符后后续检查点失效 |
| AUTO-HL-04 | P0 | Unicode 安全 | JSON、JavaScript、Python、Shell 的 emoji 与代理项不导致崩溃 |
| AUTO-LIFE-01 | P0 | 辅助窗口释放 | 编解码 controller 清理后 AppState 与文档可以释放 |
| AUTO-FOLD-01 | P1 | 折叠 | Markdown 标题和 JSON 嵌套容器范围正确 |
| AUTO-FOLD-02 | P1 | 折叠布局 | 折叠后隐藏行高度收缩，展开恢复且编辑不残留旧状态 |
| AUTO-PERF-01 | P1 | 4 MB 行索引 | 当前基准机器 5 秒内完成 |
| AUTO-PERF-02 | P1 | 4 MB 查找 | 当前基准机器 3 秒内完成 |
| AUTO-PERF-03 | P1 | 可视区高亮 | 当前基准机器 3 秒内完成 |
| AUTO-PERF-04 | P1 | 可视区属性批量应用 | 当前基准机器 3 秒内完成，颜色属性写入正确 |
| AUTO-PERF-05 | P1 | 10 MB 可视区高亮 | 200,000 行文件中段的可视区 Token 在 3 秒内完成 |
| AUTO-PERF-06 | P1 | 增量高亮缓存 | 10 MB 首次检查点构建在 3 秒内，相邻视口复用在 1 秒内 |
| AUTO-PERF-07 | P1 | TextKit 可视区布局 | 4 MiB 首屏布局在 3 秒内，中段布局在 5 秒内 |
| AUTO-PERF-08 | P1 | 输入模型同步 | 10,000 次输入通知在 1 秒内，期间不生成 10 MB 完整模型快照；按需同步在 2 秒内 |
| AUTO-PERF-09 | P1 | 原生文本行列定位 | 10 MB 可变文本存储上的 10,000 次行列定位在 1 秒内 |
| AUTO-PERF-10 | P0 | 50 MB 打开与搜索 | UTF-8 解码小于 2.5 秒，搜索小于 1.5 秒，档位为 large |
| AUTO-PERF-11 | P1 | 100 MB 尽力打开 | UTF-8 解码小于 6 秒，不崩溃，档位为 extreme |
| AUTO-PERF-12 | P1 | 1/10/20 MB 形态覆盖 | 短行代码、超长行日志和 Markdown 均可后台读取并正确分档 |
| AUTO-PERF-13 | P1 | 列表局部重排 | 20 万行文档末端的连续列表在 250 ms 内完成重排 |
| AUTO-PERF-14 | P1 | 行号布局缓存 | 同一视口重复绘制复用可见行映射；滚动、缩放和内容更新后缓存正确失效 |
| AUTO-PERF-15 | P1 | 深位置可视区布局 | 10 MB 代码文件在 10%、50%、90% 位置均使用非连续布局，单次可视区布局小于 1 秒 |
| AUTO-PERF-16 | P1 | 深位置窗口缩放 | 10 MB 代码文件连续改变 19 次容器宽度后，90% 位置重新布局小于 1 秒 |
| AUTO-PERF-17 | P1 | 高亮取消续扫 | 深位置词法扫描被连续取消时保留已完成检查点，下一次任务从更后位置继续 |
| AUTO-PRIV-01 | P0 | 权限与网络 | 无网络 entitlement、URLSession 或 Network API |

性能阈值是防止数量级回退的宽松门槛，不用于产品间跑分。基准机器或 SDK 变化时，应同时记录旧值和新值后再调整。

## 4. 人工功能用例

### 文件与编码

| ID | 操作 | 预期结果 |
| --- | --- | --- |
| UI-FILE-01 | 新建内容，保存，再关闭重开 | 内容完整，标题和最近文件更新，状态为“已保存” |
| UI-FILE-02 | 执行“另存为”并选择不同扩展 | 原文件不被覆盖，新标签路径、语言和图标更新 |
| UI-FILE-03 | 同一窗口重复打开同一文件 | 定位已有标签，不创建副本 |
| UI-FILE-04 | 拖入 TXT、MD、HTML、JSON、JS | 文件正常打开，语言模式正确 |
| UI-FILE-05 | 打开非 UTF-8 文件 | 显示编码选择；取消不创建标签，正确编码可打开 |
| UI-FILE-06 | 修改后关闭标签、窗口和退出 | 每种路径均出现保存/不保存/取消确认 |
| UI-FILE-07 | 清空新建文档后关闭 | 不提示保存；唯一空白标签再次关闭时关闭窗口 |
| UI-FILE-08 | 最近文件右键菜单 | 打开、新窗口打开、重命名、Finder 定位、移除均正确 |
| UI-SIDEBAR-01 | 收藏最近文件并打开第二个窗口 | 收藏夹立即出现，两个窗口内容和顺序保持一致 |
| UI-SIDEBAR-02 | 创建、重命名、折叠及删除分组 | 状态正确持久化；删除分组不影响磁盘文件 |
| UI-SIDEBAR-03 | 拖动收藏、分组和组内文件 | 可调整到首位、末位和中间；跨组拖动添加快捷入口 |
| UI-SIDEBAR-04 | 从 Finder 拖入支持文件与目录 | 支持文件成功加入目标区域；目录和不支持格式被忽略 |
| UI-SIDEBAR-05 | 移动收藏文件后重新打开 App | 条目显示失效状态，“重新定位”后路径恢复且可以打开 |
| UI-SIDEBAR-06 | 分别折叠收藏夹、分组和最近文件并重启 | 三个模块独立折叠，多窗口同步且重启后保持原状态 |
| UI-FILE-09 | 在两个窗口分别打开原路径和符号链接 | 只保留一个可编辑标签，并激活已有窗口 |
| UI-FILE-10 | 外部程序修改已打开文件后保存 | 默认不覆盖；重新载入、另存为、覆盖、取消均按选择执行 |
| UI-FILE-11 | 打开 UTF-16/GB18030 后直接保存 | 文件保持原编码；加入不可表示字符时原文件不被覆盖 |

### 标签与窗口

| ID | 操作 | 预期结果 |
| --- | --- | --- |
| UI-TAB-01 | 单标签和双标签之间切换 | 单标签隐藏标签栏，双标签显示，布局平滑 |
| UI-TAB-02 | 使用 `⌘1...9`、`⌃Tab`、`⌥⌘←/→` | 切换到正确标签，无明显延迟 |
| UI-TAB-03 | 拖动标签到首位、末位和中间 | 顺序正确，末尾剩余区域均可作为落点 |
| UI-TAB-04 | 创建 10 个以上标签并滚动标签栏 | 滚轮横向移动列表，边缘渐隐准确，不切换标签 |
| UI-TAB-05 | 将标签拖出窗口再拖回 | 新窗口创建且应用不卡死，内容和未保存状态保留 |
| UI-TAB-06 | `⌘N` | 新建独立窗口，侧边栏按钮、菜单和主题均可用 |
| UI-TAB-07 | 标签右键关闭其他/右侧 | 目标集合正确，未保存文档逐一确认 |
| UI-TAB-08 | `⌘T` 后立即连续输入，再切回原标签 | 输入只进入新标签；原标签内容、保存状态和滚动位置不变 |

### 编辑、查找与工具

| ID | 操作 | 预期结果 |
| --- | --- | --- |
| UI-EDIT-01 | 输入、删除、粘贴后撤销/重做 | 内容、光标和未保存状态正确恢复 |
| UI-EDIT-02 | 全部替换后撤销 | 一次 `⌘Z` 恢复替换前全文 |
| UI-EDIT-03 | JSON 格式化/压缩后撤销 | 一次 `⌘Z` 恢复操作前全文 |
| UI-EDIT-04 | `⌘F` 打开查找，再点击正文输入 | 查找为非模态独立窗口，正文仍可编辑 |
| UI-EDIT-05 | `⌥⌘F` 原地切换替换模式，使用转义字符 | 同一面板切换，全部替换数量和结果正确 |
| UI-EDIT-06 | 自动换行开关与分栏拖动 | 开启后无水平漂移，文字不进入预览区域 |
| UI-EDIT-07 | 显示/隐藏行号和状态栏 | 布局立即更新，行号与逻辑行一致 |
| UI-EDIT-08 | `⌘+`、`⌘-`、`⌘0` | 字体在 9–32 pt 范围变化并可恢复 |
| UI-EDIT-09 | Markdown 有序列表回车、插入、删除、粘贴 | 自动续号但不重写粘贴的显式编号 |
| UI-EDIT-10 | Markdown/JSON 折叠命令 | 当前区块折叠和展开，编辑后安全展开 |
| UI-EDIT-11 | 大文件中保留离屏光标，滚动后提交普通输入和中文输入法文本 | 回到真实编辑行；光标、状态栏和行号一致，不跳到文件末尾 |
| UI-EDIT-12 | 在 10 MB 文件中输入后立即保存、查找、关闭或拖到新窗口 | 每条路径都使用最新内容；未保存标记即时出现，撤销历史不因模型同步而丢失 |
| UI-EDIT-13 | 选择 URL、Base64、HTML 实体或 Unicode 转义，使用“编辑 > 编码与解码”及右键菜单 | 对应命令可用，右键根菜单显示原生图标，结果正确且不自动修改正文 |
| UI-EDIT-14 | 将光标放在 `hello%20world` 中等待 | 状态栏显示“检测到 URL 编码”；点击后打开非模态双栏预览，正文仍可编辑 |
| UI-EDIT-15 | 在转换预览中复制结果、替换原文并执行一次 `⌘Z` | 复制板内容正确；替换只影响原范围；一次撤销恢复原文 |
| UI-EDIT-16 | 打开转换预览后修改原文，再点击替换 | 预览标记为已失效，不覆盖新输入；无选区和无效编码显示明确中文提示 |
| UI-EDIT-17 | 预览包含数百字符的连续 URL，并缩放转换窗口 | 原文与结果按面板宽度自动换行，只出现纵向滚动条，无水平滚动 |
| UI-EDIT-18 | 打开包含超长自动换行、普通换行和空行的长文档，先缓慢滚过换行首片段，再连续快速上下滚动 | 每个逻辑行只在首个视觉片段显示行号；首片段离开后行号同步离开；续行不显示、相邻数字不重叠且无旧数字残影 |
| UI-EDIT-19 | 在单标签窗口打开长文档，以很小滚动增量让一行经过标题栏边界，再反复回到首行、快速上下滚动，并切换是否显示标签栏 | 正文始终可见；行号与对应首个文字片段同步连续上移并完全离开，不会吸附顶部或等待下一行覆盖；首行不出现叠加残影 |
| UI-EDIT-20 | 打开两个代码标签，分别停在不同滚动位置；连续改变窗口宽高 5 次并在缩放过程中观察标尺，再快速往返切换标签 5 轮 | 缩放每一帧及标签重新激活后，首行都只有当前布局对应的单个行号；无旧尺寸、旧标签数字残留或重影；普通滚动保持流畅 |
| UI-EDIT-21 | 分别使用默认字体及连续放大 3 次后的字体，打开包含自动换行的代码文件并滚动到中段和标题栏边界 | 所有行号在对应正文首片段的行框内垂直居中；普通可见区域内的数字始终完整，字体越大也不产生垂直错位；仅顶部边界行号与正文同步裁切和离开 |
| UI-EDIT-22 | 在普通行、空行、自动换行的长逻辑行和文件最后一行单击行号 | 每次都选中完整逻辑行（含换行符）；自动换行的续行一并被选中，行号实时呈现激活状态 |
| UI-EDIT-23 | 从任意行号向上、向下拖至多行，并继续拖到编辑区顶部或底部边缘 | 选区随鼠标实时扩展或收缩；逻辑行范围完整、可反向拖动；到达边缘后继续滚动和选择，行号与正文不脱节 |
| UI-EDIT-24 | 打开 10 MB 自动换行代码，连续调整窗口宽度、高度和角点 20 秒，再立即滚动、输入和切换标签 | 缩放过程保持连续响应，正文与行号同步变化且无重影；松手后高亮补齐，滚动、输入和标签切换无任务积压 |
| UI-EDIT-25 | 在 10 MB 代码文件的 10%、50%、90% 位置分别快速滚动并连续改变窗口宽度 | 越靠近文件末尾也不会出现递增卡顿；缩放期间保持响应，松手后行号与高亮准确恢复 |

### 预览、高亮与状态

| ID | 操作 | 预期结果 |
| --- | --- | --- |
| UI-MD-01 | 编辑含标题、列表、引用、链接、代码、表格的 MD | 预览约 180 ms 后更新为最新内容，无旧序号残留 |
| UI-MD-02 | 快速连续输入 20 次 | 最终预览与编辑器最后内容一致 |
| UI-MD-03 | 拖动预览分隔线并缩到最小窗口 | 两侧不重叠、不遮挡，均保持最小可用宽度 |
| UI-MD-04 | 连续开关 Markdown 预览 10 次并在每次切换后输入 | 正文始终可见且位置不跳动；行号、光标、输入和预览内容保持正确 |
| UI-MD-05 | 编辑包含二级、三级无序列表以及有序/无序混排的 Markdown | 子列表保持在对应父项内，缩进清晰；无序层级依次使用实心圆、空心圆和方形，有序层级编号正确 |
| UI-MD-06 | 在 Markdown 预览中右键选择“重新载入” | 最近一次完整预览立即重新显示，不白屏，也不需要修改正文触发刷新 |
| UI-HL-01 | 打开每种代码格式 | 语言名称、图标和基础语法颜色正确 |
| UI-HL-02 | 在字符串中输入注释符，在注释后换行 | 字符串不变注释色，下一行恢复正常高亮 |
| UI-HL-03 | 在代码模式中将光标放到 `()`、`[]`、`{}` 任一端或单独选中一个括号 | 正确配对的两个字符同时以低饱和背景高亮；光标放在括号前后均可触发，不滚动正文 |
| UI-HL-04 | 将括号写入字符串、行/块注释、JS 模板字符串和正则，再测试 `([)]` 与缺失闭合符 | 伪括号和错误结构不产生配对；未匹配时不显示警告或残留高亮 |
| UI-HL-05 | 在 50 MB JSON 中连续移动到 100 个括号并快速切换标签、输入中文 | 编辑保持响应，只显示最后一次有效结果；IME 与失活标签无残留高亮或任务积压 |
| UI-JSON-01 | 格式化无效 JSON | 内容不改写，状态栏显示错误并尽量包含行列 |
| UI-STATUS-01 | 移动光标和编辑中英文 | 保存状态、UTF-8、语言、字数、行列实时正确 |

### 外观、菜单与稳定性

| ID | 操作 | 预期结果 |
| --- | --- | --- |
| UI-VIEW-01 | 浅色、深色、跟随系统依次切换 | 背景、文字、预览和设置均可读，无残留错误颜色 |
| UI-VIEW-02 | 展开/收起侧边栏并重启验收 App | 状态被记住，标题和第一行行号不跳动 |
| UI-VIEW-02A | 使用 `⌃⌘B` | 显示或隐藏侧边栏；工具栏 Tooltip 同步显示 `⌃⌘B`，且不影响 `⌘S` 保存 |
| UI-VIEW-03 | 收起侧边栏后先沿编辑区左边缘移动，再悬停按钮、移入抽屉并点击按钮固定 | 左边缘不触发抽屉；仅悬停按钮时临时出现，移入抽屉后保持；固定时侧边栏保持原位，仅编辑区平滑让位，不发生先收起再展开；移开未固定抽屉后稳定消失 |
| UI-VIEW-04 | 展开“显示 > 外观”等二级菜单 | 子菜单可通过鼠标悬停打开，图标与文字对齐 |
| UI-VIEW-05 | 悬停所有工具按钮至少 2 秒 | Tooltip 稳定显示；有快捷键时同时显示快捷键 |
| UI-VIEW-06 | 打开设置窗口，调整字体后重启并点击“恢复默认” | 内容完整且三种主题下均可读；字体设置被保留，恢复后为 14 pt |
| UI-VIEW-07 | 在浅色和深色模式下分别检查单标签、多标签、侧边栏展开与收起 | sidebar 材质与正文层级清晰；单标签不显示标签栏；标签、标题、编辑器和状态栏没有跳动或重叠 |
| UI-VIEW-08 | 开启 macOS“减少动态效果”后切换侧边栏、标签栏和状态栏 | 不使用位移或缩放动画，编辑器切换仍即时，内容与焦点保持正确 |
| UI-VIEW-09 | 检查并折叠收藏夹、分组、最近文件及自定义分组 | 顶层箭头仅悬停时出现在名称与数量之后；自定义分组以空心、实心文件夹区分状态；内容只做上下收缩，不淡入淡出 |
| UI-STAB-01 | 打开并编辑 4 MB 文件 2 分钟 | 输入和滚动可用，无卡死或异常内存增长 |
| UI-STAB-02 | 连续切换 10 个标签 50 次 | 无崩溃、内容串页、明显逐次变慢 |
| UI-STAB-03 | 打开 10 MB / 200,000 行代码后快速滚动 30 秒 | 界面保持响应，停止滚动后高亮补齐，无持续积压或整段错误颜色 |
| UI-STAB-04 | 在 3 个大文件标签间连续切换 50 次 | 热标签保留选区和滚动位置，切换无明显停顿或内存持续增长 |
| UI-STAB-05 | 打开 50 MB 文件并编辑、保存、查找、跳转中段 | 自动显示大文件模式；界面无持续冻结，保存后内容完整 |
| UI-STAB-06 | 打开超过 50 MB 文件并确认继续 | 仅警告一次，进入超大文件保护模式，高亮与其他高成本功能默认暂停 |
| UI-STAB-07 | 大文件模式临时恢复换行、预览或高亮 | 只影响当前标签；关闭后重新打开恢复自动管理状态 |
| UI-STAB-08 | 保存或全部替换期间继续输入 | 旧任务不覆盖新内容，保存完成后仍正确显示未保存状态 |
| UI-STAB-09 | 10 个大标签切换 100 次并模拟内存压力 | 当前标签不释放；非活动缓存回落，选区和滚动可恢复 |
| UI-STAB-10 | 在隔离验收 App 中编辑未保存内容后强制结束进程，再重新启动 | 自动恢复全部未保存标签；正文、文件名、编码、选区和大致滚动位置正确，标签保持未保存状态 |
| UI-STAB-11 | 正常保存、选择“不保存”关闭及正常退出后重新启动 | 已处理的文档不会再次出现恢复提示，也不会产生空白恢复标签 |
| UI-STAB-12 | 异常退出后先用其他应用修改原文件，再启动并保存恢复标签 | 默认停止覆盖并显示外部修改冲突，可重新载入、另存为或明确覆盖 |

## 5. 回归与报告规则

发现缺陷后必须：记录失败用例 ID和复现步骤；修复源码；为可自动化的问题增加回归测试；重新执行失败用例和完整自动门禁。验收报告应区分“首次通过”“修复后通过”“已知限制”，不得删除失败历史来制造全绿结果。
