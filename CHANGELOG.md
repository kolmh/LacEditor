# LacEditor 更新日志

这里记录 LacEditor 每个正式版本中用户可以感知的变化。

日志遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的组织方式，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。更新内容使用面向用户的中文描述，
不直接罗列提交记录。

## [未发布]

暂无未发布内容。

## [0.13.1] - 2026-09-20

### 修复

- 修复从 Finder 选择“用 LacEditor 打开”时错误创建新窗口并关闭现有工作区的问题。
- Finder 打开文件请求现在会复用当前编辑窗口，保留已有标签和未保存内容；应用启动尚未完成窗口注册时会暂存请求，待窗口就绪后再打开。

## [0.13.0] - 2026-09-03

### 新增

- Finder 可将 TXT、Markdown、HTML、JSON 以及常见代码文件直接用 LacEditor 打开。
- 应用声明为这些文本和源代码格式的编辑器，并接收 Finder 的文件打开事件。

### 修复

- 修复 Markdown 预览无法显示普通单换行的问题。
- 保留代码块、列表、表格和段落的原有结构，不把空行误处理成额外换行。

## [0.12.0] - 2026-08-28

本版本将复杂代码语言的语法解析逐步统一到 Tree-sitter，在保持 JSON、Markdown 和大文件专用路径的同时，
改善代码高亮准确性、增量编辑性能和 Unicode 文本的范围定位。

### 新增

- Swift、Shell、YAML、C/C++ 和 SQL 文件接入 Tree-sitter 语法解析与高亮。

### 优化

- 编辑器高亮开始按编辑影响范围增量刷新；输入时保留未受影响区域的语法颜色，减少长文档重复扫描和闪烁。
- 首次打开或滚动时优先处理可视区域及少量上下文，后台任务完成后再逐步覆盖其他区域。
- 查找结果按文档 revision、查询内容和大小写选项缓存，连续执行“查找下一个/上一个”时复用匹配范围。
- 语法高亮增加可替换解析后端边界，支持按语言选择解析器，不改变现有编辑器和大文件保护路径。
- JavaScript 在 20 MB 以内启用 Tree-sitter 解析，并使用按文档缓存的增量语法树；连续编辑通过 `InputEdit`
  更新，中文和 emoji 的高亮范围统一按 UTF-16 坐标转换。其他语言仍按各自专用解析器或增量词法扫描器处理，
  同一文档不会同时运行两套代码解析器。
- TypeScript 在相同的文件大小门槛下接入独立 Tree-sitter grammar，支持接口、类型标注、泛型等 JavaScript
  grammar 无法准确表达的语法，并复用同一套增量缓存和取消机制。
- Python 在相同门槛下接入 Tree-sitter grammar，改善缩进块、装饰器、字符串、数字和注释边界的识别，
  并复用按文档和语言隔离的增量语法树。
- CSS 在相同门槛下接入 Tree-sitter grammar，改善选择器、属性名、颜色值、尺寸值和注释的识别，
  并复用同一套按语言隔离的增量语法树缓存。
- HTML 在相同门槛下接入 Tree-sitter grammar，改善标签、属性、属性字符串、注释和文档类型的识别；
  脚本与样式内容仍由编辑器原有语言路由处理，避免同一文档重复构建完整语法树。
- Swift、Shell、YAML、C/C++ 和 SQL 在相同门槛下接入 Tree-sitter grammar，统一使用按文档和语言隔离的增量
  语法树缓存；每个文档只启用一个解析后端，避免旧扫描器与 Tree-sitter 重复处理同一份内容。

### 测试

- 新增编辑脏范围在插入、删除、连续编辑、零长度编辑以及中文和 emoji UTF-16 坐标下的回归测试。
- 新增 JavaScript Tree-sitter 的注释边界、字符串、中文/emoji UTF-16 范围及增量编辑回归测试。
- 新增 TypeScript 的类型节点、Unicode 标识符、注释边界回归测试。
- 新增 Python 缩进、字符串、Unicode 标识符、注释边界和增量编辑回归测试。
- 新增 CSS 选择器、属性、颜色、尺寸值和注释边界回归测试。
- 新增 HTML 标签、属性、属性字符串、Unicode 文本和注释边界回归测试。
- 新增 Swift、Shell、YAML、C/C++ 和 SQL 的注释边界、字符串/标量、Unicode 文本及语法树路由回归测试。

## [0.11.0] - 2026-08-27

本版本完善缩进与多级 Markdown 列表编辑，并从底层统一编辑器字体、固定行高、输入法组合文字与
语法着色管线，解决中英文输入时行高或字体跳变，以及 Markdown 偶发整段变色的问题。

### 新增

- 设置新增“缩进方式”和“Tab 宽度”，可在空格缩进与真实 Tab 字符之间切换；调整 Tab 宽度后，
  当前文档中已有的 Tab 会统一按新宽度重新显示，不改写文件内容。
- Markdown 有序列表支持多级嵌套：在列表项中使用 Tab 或 Shift+Tab 可降级、升级，并自动重排
  当前层级及后续同级序号。

### 优化

- 编辑器改用 Menlo 作为拉丁字符基础字体，并由 macOS 原生字体回退绘制中文；不打包第三方字体，
  英文和数字保持等宽，中文继续使用系统字形。
- 行高改为由 TextKit 布局器统一根据字体指标和用户设置的行距计算，普通行、自动换行、空白末行、
  中文输入法组合文字和中英混排行使用同一行框与基线。
- 设置页面支持纵向滚动，较小窗口中也能访问全部编辑器选项与恢复默认操作。
- 语法着色改为显示层的临时属性，并将一次高亮中的颜色变化合并提交，减少大段 Markdown 或代码
  重新着色时的闪烁和正文属性写入。

### 修复

- 修复先输入英文再输入中文、中文输入法上屏或在末尾空行开始输入时，整行高度、基线或字体会突然
  变化的问题；输入法不再触发正文整行字体重写。
- 修复 Markdown 编辑时偶发整段文字变成蓝色、需要再次编辑才恢复，以及标题标记 `#` 相对正文
  上移的问题；标题、强调和注释等高亮不再改变字体或段落布局。
- 修复多级有序列表升级后序号或点号后的空格错误，以及列表项变更层级后后续同级编号未立即更新的问题。

## [0.10.0] - 2026-08-14

本版本加入正常退出后的完整工作区恢复，并允许在设置中选择退出方式；同时改进 JSON 格式化、
编辑器行距和当前行辨识度，让日常退出、重新打开和代码阅读更加自然可靠。

### 新增

- 新增正常退出工作区恢复：使用 `Command+Q` 或 Dock“退出”时，可保留全部窗口、标签顺序、
  当前标签、正文、文件信息、未保存状态、选区、滚动位置、Markdown 预览、侧边栏状态和窗口位置，
  下次启动自动还原；工作区快照与异常退出恢复独立存储，不会自动改写磁盘文件。
- 设置新增“退出行为”，可在默认的“保留工作区并退出”和传统的“每次检查未保存文件”之间切换；
  第一次带有未保存内容退出时，会明确提供保留、逐项检查和取消三种选择。

### 优化

- JSON 格式化与压缩改为单遍保序扫描，不再构建字典或按字段名排序；字段顺序、重复键、数值写法
  和字符串转义保持原样，同时降低大文件处理的排序开销与内存峰值，并支持扫描过程中取消。
- 编辑器正文与代码默认额外行距增加至 4 pt，并可在设置中于 0–10 pt 间调整或一键恢复默认；
  行号、自动换行和折叠布局继续使用同一套行框保持对齐。
- 加深当前行的低饱和靛蓝底色，让光标所在行在浅色和深色模式下都更容易辨认。

## [0.9.0] - 2026-08-07

本版本升级 Markdown 预览内核并加入异常退出恢复，重构多窗口编辑器会话与拖拽组件，同时解决
长代码文档滚动越靠后越卡、窗口缩放延迟和深位置语法扫描重复执行的问题。运行环境调整为
macOS 26 与 Apple Silicon。

### 新增

- Markdown 预览改用成熟的 `cmark-gfm` 解析器，完整支持 CommonMark，并加入 GFM 表格、任务列表、
  删除线、自动链接和标签过滤扩展。
- 新增真实窗口、跨窗口标签拖放、编辑器会话缓存和内存压力回归测试。
- 新增自动恢复保护：未保存内容在停止输入后写入独立恢复副本，应用异常退出后会在下次启动时
  自动恢复；正常保存、放弃修改、关闭窗口或正常退出不会留下待恢复内容。
- 新增项目构建产物清理脚本，默认只显示目标和预计空间，必须使用工程绝对路径明确确认后才会执行。

### 优化

- 中大型文档统一使用 TextKit 非连续可视区布局，滚动到文件后半段时不再从文档开头补齐布局；
  自动换行状态下调整窗口宽度，也只重新计算当前可见区域。
- 窗口实时缩放的正文与行号布局更新合并到每秒最多 60 次，并统一使用非连续可视区布局；行号仍随
  每次实际正文布局同步更新，松开窗口边缘后再执行一次精确刷新。
- 代码高亮改为固定 64 KiB 分块缓存，同一分块内滚动不再重复改写文本属性；深位置词法扫描即使
  被快速滚动取消，也会保留已经完成的检查点并从该位置继续。
- Markdown 的松散列表、`1)` 有序列表、复杂续行和表格转义现在遵循 CommonMark/GFM 规则；
  原始 HTML 与危险链接协议继续由预览安全边界拦截。
- 编辑器、标签栏和侧边栏的大型实现拆分为职责明确的协调器、会话缓存、拖拽、滚动和资料库视图，
  降低后续维护与回归风险。
- 编辑器缓存测试覆盖 LRU 数量、内存预算、超大会话和 critical 内存压力，确保活动编辑器不会被淘汰。
- 恢复内容保留原文件编码和磁盘版本基线；恢复后仍需用户明确保存，磁盘文件在异常退出后被其他程序
  修改时会继续触发现有冲突保护，不会由恢复流程静默覆盖。

### 变更

- 显示或隐藏侧边栏的快捷键由 `Control+Command+S` 调整为 `Control+Command+B`，避免与保存相关
  快捷键使用相同字母，并与 Sidebar 的含义对应。
- 目标平台调整为 macOS 26.0 及以上、Apple Silicon `arm64`，构建环境调整为 Xcode 26；
  不再生成 Intel 或旧版 macOS 兼容产物。

## [0.8.0] - 2026-08-06

本版本重新整理行号标尺与窗口缩放流程，提升自动换行、快速滚动、标签切换和调整窗口尺寸时的
对齐稳定性与流畅度，并进一步精简侧边栏。

### 新增

- 行号支持整行选择：单击行号可选中完整逻辑行，按住并上下拖动可连续选择多行；同时支持
  自动换行内容、反向拖动、拖至边缘后继续选择，以及 Shift 点击扩展已有整行选择。

### 优化

- 侧边栏的收藏夹、分组和最近文件顶层标题移除图标，减少重复视觉元素，保留数量和悬停时显示的折叠箭头。
- 移除侧边栏顶部的“文件”标题和新建分组按钮，让资料库内容直接开始显示，减少无效占位。
- 行号标尺改为由 TextKit 的同一份可见逻辑行映射驱动：绘制、当前行状态和鼠标命中不再各自计算位置，
  在自动换行、滚动、缩放和切换标签时保持一致。
- 窗口实时缩放改为每个主线程周期最多提交一次布局更新；缩放期间暂停语法高亮与方向预热，
  不再同步等待行号绘制，松开窗口边缘后再执行一次精确布局，显著降低连续缩放时的卡顿。
- 行号标尺缓存同一视口的可见逻辑行映射，重复绘制、鼠标命中和选区反馈不再反复请求
  TextKit 布局；滚动、正文、折叠、字体或宽度变化时会自动失效。

### 修复

- 修复快速滚动或自动换行时，行号标尺顶部补绘层可能遮住相邻行号、首行可能残留旧数字的问题；
  标尺现在按窗口真实内容区域裁切，无标签栏时也会与正文一起避开标题栏。
- 修复行号标尺回退到系统默认绘制后可能覆盖编辑区域、导致正文不可见的问题；标尺现在始终
  使用受控的自定义绘制入口，行号裁切不会改变编辑区的渲染。
- 优化标题栏边界处的行号裁切方式：数字与正文一样逐步被边界遮挡，不再在中心点越界后整行消失。
- 修复自动换行时行号未严格绑定逻辑行首个视觉片段、滚动后可能滞留或覆盖相邻行号的问题；
  首片段离开可见区域后行号会同步离开，续行不重复显示。
- 修复改变窗口尺寸或切换标签后，旧布局的首行数字可能残留并与当前行号重叠的问题；失活标签
  会取消旧标尺任务，缩放稳定及标签重新激活后会完整清除并重绘标尺。
- 修复顶部行号通过跨视图坐标转换后可能被固定在可见边界、直到下一行到达才消失的问题；
  行号位置现在直接跟随对应首个文字片段和编辑器滚动偏移连续移动。
- 行号滚动和窗口缩放刷新改为同一主线程周期内同步重绘，并在缩放期间禁用旧标尺 backing 内容
  的拉伸保留，避免延迟刷新期间仍看到首行重影或位置滞后。
- 修复误将 TextKit 从行框底部计算的基线偏移当作顶部偏移、导致行号与正文错位的问题；行号
  现在严格在对应正文首片段的行框内垂直居中，不再单独推算基线。
- 修复基线对齐后仍将行号裁切到正文行片段高度、导致普通可见区域内数字显示不完整的问题；
  现在只在编辑区与标题栏的整体边界进行裁切。
- 修复行号与正文行框的垂直位置不一致、顶部行号可能与下一行发生视觉覆盖的问题；行号现在始终在
  对应正文首个视觉片段内居中，并与正文一起滚动和裁切。

## [0.7.0] - 2026-08-06

### 新增

- 设置页底部新增当前应用版本与构建号，信息直接读取自 App 构建配置，版本升级后会自动更新。
- 新增应用级文件身份与磁盘版本基线；同一磁盘文件通过原路径、符号链接或不同窗口打开时，
  会直接定位到唯一的编辑标签。
- 新增外部修改冲突处理，可选择重新载入、另存为、明确覆盖或取消，默认不会覆盖磁盘新版本。
- 新增 XCTest 稳定性回归，覆盖文件身份、UTF-16/GB18030 往返、无损保存、Unicode 词法边界、
  编解码限制和辅助窗口释放。
- 新增应用级共享设置状态；主题、字体、自动换行、行号和状态栏会在所有已打开窗口间即时同步，
  自动换行设置也会在重启后保留。
- 左侧侧边栏新增收藏夹与自定义分组：文件可以收藏、加入多个分组，分组支持创建、重命名、
  折叠、删除和拖动排序；所有窗口共享同一份本地资料库。
- 收藏与分组支持内部拖动排序，也可以从最近文件或 Finder 直接拖入；删除分组或快捷入口不会
  删除磁盘文件。
- 收藏夹、分组和最近文件三个顶层模块均可独立折叠，展开状态会在所有窗口间同步并在重启后保留。

### 优化

- 侧边栏顶层模块的折叠箭头移到名称与数量之后，并保持仅悬停标题行时显示；分组使用空心、
  实心文件夹区分折叠与展开状态，内容改为无淡入淡出的垂直收缩动画。
- 打开非 UTF-8 文件时，仅编码选择留在主线程，实际解码和行数统计回到后台；文件打开任务支持取消。
- 保存和多文档关闭改为异步顺序流水线，保存期间仍可编辑，失败或取消会立即停止后续关闭。
- URL 编码改为预分配的单字节缓冲构建；8 MB 以上转换会提示预计内存，32 MB 以上拒绝执行。
- 查找与替换任务绑定发起时的文档，切换标签、修改内容或关闭窗口会取消旧任务。
- 代码语法高亮统一使用 `CodeLexicalScanner` 的单一增量词法状态，删除约 700 行重复扫描实现。
- Markdown 列表识别复用预编译正则，JSON 折叠直接读取原文本存储，不再复制整份 UTF-16 数组。

### 修复

- 修复侧边栏收藏夹、分组、最近文件或自定义分组收起后仍保留原内容高度，导致模块之间出现
  大块空白的问题；折叠后现在会立即释放布局空间。
- 修复侧边栏展开或收起时文件列表以位移动画穿过模块标题、造成内容重叠的问题；现在使用
  标题下方的垂直裁切过渡。
- 修复 Markdown 预览中右键“重新载入”后页面变为空白的问题；现在会重新显示最近一次完整预览。
- 修复 Markdown 二级、三级列表被错误渲染成一级列表的问题，并统一处理有序与无序列表混排、
  缩进续行及不同层级的项目符号样式。
- 修复非 UTF-8 文件保存时被静默改写成 UTF-8 的问题；现在普通保存和另存为默认保留原编码，
  无法表示新字符时不覆盖原文件，并提供另存为 UTF-8。
- 修复语法扫描遇到 emoji 的 UTF-16 代理项时可能因强制解包而崩溃的问题。
- 修复辅助窗口按 `Command+W` 误关闭编辑标签，以及编解码窗口可能持续持有整个文档窗口的问题。
- 修复保存失败后状态可能永久停留在“正在保存”，以及失败的打开标签无法直接重试的问题。
- 移除侧边栏不再使用但仍残留的“打开文件夹”菜单、文件树模型和同步递归扫描，避免大型目录
  造成无意义的主线程停顿。
- 收藏或分组中的文件被移动、删除后会显示失效状态，并提供“重新定位”；在 LacEditor 内重命名
  文件时，收藏、分组和最近文件路径会一起更新。

## [0.6.0] - 2026-08-05

本版本新增面向日常开发的文本编码与解码工具，可智能识别常见编码内容，在不阻断编辑的
预览窗口中确认结果，并通过范围替换和过期校验保护大文件性能与新输入。

### 新增

- 新增统一的“编码与解码”工具，支持 URL 组件、Base64、Base64URL、HTML 实体及
  Unicode/JSON 转义的编码和解码；URL 解码区分标准模式与将 `+` 视为空格的表单模式。
- 编辑器可在选区中智能识别 URL、Base64、HTML 实体和 Unicode 转义；未选择文本时仅检测
  光标所在的 URL、HTML 实体和 Unicode 转义，以降低 Base64 误判，并通过状态栏、原生编辑
  菜单及右键菜单提供解码入口。
- 新增非模态转换预览窗口，并排显示原文和结果；用户可以复制结果或确认替换，预览期间
  仍可操作正文。

### 优化

- 编解码仅提取和替换实际选区，不复制或重写整份文档；较大转换在后台执行，并通过编辑器
  revision 与原文校验阻止过期结果覆盖新输入。
- 自动识别限制在当前选区或光标所在文本的 16 KiB 范围内，并对 Base64 使用 UTF-8、
  可显示字符比例和最短长度检查，降低普通代码被误判的概率。
- 编解码预览中的长 URL 和连续文本改为随面板宽度自动换行，只保留纵向滚动；编辑器右键
  菜单的“编码与解码”入口补充与主菜单一致的原生图标。

### 修复

- 编解码写回作为单次原生撤销操作提交；转换期间继续编辑时，旧预览会失效且不会修改正文。

## [0.5.0] - 2026-08-04

本版本集中提升原生 macOS 界面的完整度与编辑可靠性，重新整理侧边栏、标签栏和状态栏视觉，
补充字体设置与代码括号配对高亮，并修复 Markdown 预览、未保存状态和侧边栏交互问题。

### 新增

- 新增代码括号配对高亮：光标位于 `()`、`[]`、`{}` 任一端时，会以低干扰强调色同时
  标记正确配对；匹配会跳过字符串、注释、JavaScript 模板字符串和正则表达式。
- 设置页的编辑器字体选项新增“恢复默认”按钮；字体大小会在应用重启后保留，默认值为 14 pt。

### 优化

- 侧边栏改为单一持续挂载的 hidden、preview、pinned 三态视图；从悬停临时抽屉点击固定时，
  侧边栏保持原位，仅让编辑区平滑让位并淡出阴影，不再先收起再重新展开。
- 统一窗口视觉规范：侧边栏改用原生 macOS sidebar 材质，标题栏、标签栏、编辑区和状态栏
  使用一致的背景、间距、圆角与紧凑尺寸，整体层级更接近 Finder 风格的原生编辑器。
- 标签栏使用更轻的激活描边和悬停反馈，未保存标记与关闭按钮共用尾部位置；缩短溢出渐隐，
  减少标签较多时的视觉噪声。
- 最近文件侧边栏降低标题存在感，压缩文件行高，并为已打开文件增加细强调标记；临时抽屉
  使用轻微边缘阴影，与固定展开状态清晰区分。
- 状态栏改为左右信息分组，移除连续竖向分隔线；编辑器当前行强调色进一步降低饱和度，
  长时间阅读更平静。
- 结构动画统一为 160–180 ms，悬停反馈约 90 ms；标签切换不参与布局动画，系统开启
  “减少动态效果”时停用位移和缩放，兼顾流畅度与辅助功能。

### 修复

- 修复侧边栏收起后，鼠标经过编辑区左边缘也可能触发临时抽屉的问题；现在只有悬停工具栏
  的“显示侧边栏”按钮才能打开预览，临时侧边栏自身只负责维持悬停和延迟关闭。
- 修复 Markdown 预览开关在普通编辑器与分栏视图之间移动同一个原生编辑器会话，导致关闭或
  重新开启预览后正文仍存在但 TextKit 布局位置错误、内容不可见的问题。
- 修复编辑器同步实时文本后更新未保存状态时，`@Published` 回调重入导致 Swift 运行时报告
  “Simultaneous access”并在 Xcode Debug 中暂停的问题。

## [0.4.0] - 2026-08-03

本版本集中完成大文件性能架构升级，使 LacEditor 可流畅处理最高 50 MB 的日常文本与代码文件，
并通过自动保护模式、后台任务和内存管理降低大文件对编辑体验的影响。

### 新增

- 新增自动大文件模式：超过 20 MB 或 25 万行时自动关闭高成本功能；超过 50 MB 时进入
  更严格的保护模式，并在打开前明确提示。
- 状态栏和“显示 > 大文件模式”菜单新增当前标签的性能选项，可临时恢复自动换行、
  Markdown 预览、语法高亮、代码折叠和实时字数统计，不改变全局设置。
- 打开、保存、搜索、全部替换、JSON 处理和 Markdown 渲染新增统一任务状态、取消入口与
  Instruments 性能观测点。

### 优化

- 20 MB 以上文件使用内存映射方式后台读取；保存改为后台原子写入，并通过内容 revision
  校验避免保存期间继续编辑后被错误标记为“已保存”。
- 保存进行中关闭窗口或退出应用时，改用 macOS 延迟关闭流程等待后台写入完成；等待期间
  主线程保持响应，随后再继续未保存内容确认。
- 查找、反向查找、全部替换和 JSON 格式化改为可取消的文档级后台任务；过期结果不会覆盖
  新输入，全部替换与格式化仍保持单次撤销。
- 编辑器会话缓存同时按数量和估算内存成本执行 LRU：非活动缓存总预算 64 MB，单个超过
  24 MB 的会话不保留；系统内存压力升高时自动释放非活动会话并恢复选区与滚动位置。
- 大文件启用 TextKit 非连续布局，停止滚动 120 ms 后仅预布局滚动方向前方一个视口；
  临时恢复自动换行时停止预热，避免跨区布局。
- Markdown HTML 改为串行后台生成并校验任务代次；大文件手动开启预览时使用 500 ms 防抖，
  刷新后恢复原有滚动比例。
- Markdown 有序列表只扫描编辑位置所属的连续列表，不再拆分和遍历整份文档。
- 自动性能门禁新增 1/10/20/50/100 MB 可重复生成夹具，覆盖短行代码、超长行日志、
  Markdown、JSON、50 MB 搜索和 100 MB 尽力打开。

- 大型 UTF-8 文件改为在后台完成读取和解码，文件选择后窗口不会再被磁盘读取长时间阻塞；
  非 UTF-8 文件仍会在主线程显示原生编码选择窗口。
- 语法高亮改为后台计算 Token、主线程批量应用颜色，并使用内容版本和任务代次校验丢弃
  已经过期的结果；快速输入、滚动或切换标签时只允许最新任务更新编辑器。
- 大文件会缓存当前可视区周围的高亮范围，视口仍在缓存内时不再重复扫描；快速滚动停止后
  自动补齐最终位置的高亮。
- 行号标尺的滚动刷新改为按主线程循环合并，移除每次滚动后的重复重绘，降低快速滚动时的
  TextKit 布局和绘制压力。
- SwiftUI 窗口现在只挂载当前标签的编辑器；最近使用的 3 个原生 AppKit 编辑会话脱离视图层
  缓存，切回时直接恢复选区、滚动位置、撤销历史和折叠状态，避免透明叠放多个大型文本视图。
- JavaScript、TypeScript、CSS、Python、Swift、Shell、YAML、C/C++ 和 SQL 高亮改为增量词法
  检查点：约每 16 KiB 保存跨行注释或字符串状态，滚动时从最近检查点继续，编辑后只使受影响
  位置之后的状态失效；超过 64 KiB 的跨行结构也能正确着色。
- 文件内容、光标、选区和字数变化不再统一转发给整个窗口；标签、工具栏、状态栏和编辑器
  分别订阅所需状态，持续编辑时减少无关 SwiftUI 视图刷新。
- 大文件输入改为由原生文本存储实时持有内容，完整模型快照只在高亮、保存、查找、关闭、
  跨窗口移动或短暂停顿时按需同步；未保存标记仍会在输入后立即出现，避免每次按键复制全文。
- 行号、当前行底色和光标行列直接读取 TextKit 的可变文本存储，滚动重绘不再通过
  `NSTextView.string` 桥接整份文件；高亮滚动也会复用已同步快照。
- 大文件字数统计增加空闲防抖，连续输入时取消旧任务并等待文本稳定后再扫描，避免后台反复
  处理很快就会过期的全文版本。
- 性能门禁扩展到 10 MB / 200,000 行代码、增量检查点复用、TextKit 首屏与中段布局、可见区
  属性批量应用、后台 UTF-8 预读取、10,000 次输入通知和 10,000 次行列定位，防止后续迭代
  重新引入大文件性能退化。
- 增加 Instruments Points of Interest 观测点，可分别分析文件读取、编辑器创建或复用、
  TextKit 可视区布局、Token 计算、颜色属性应用、模型同步和行号绘制耗时。
- 单标签窗口的正文贴近顶部，多标签窗口保留轻微顶部留白；正文左右内边距移除，
  文本空间更加紧凑。
- 多行选区会同步突出所有对应行号；自动换行时，行号固定在首个视觉行，当前行底色覆盖
  整条逻辑行的所有换行片段。
- 多标签窗口中的首行行号改为与正文视觉居中对齐；鼠标拖选多行时，行号高亮会随选区
  实时更新，无需等待松开鼠标。
- 行号改为在标尺内水平、垂直居中，移除右侧分隔线；当前行使用更清晰的文字颜色，
  其他行号降低对比度，浏览长文档时更容易定位光标所在行。
- “全部替换”、单处替换、JSON 格式化和压缩现在通过原生编辑器执行，可使用标准撤销命令
  恢复操作前的内容。
- 保存成功后不再在状态栏重复显示第二条“已保存”，底部信息保持紧凑清晰。
- Markdown 预览工具按钮补充明确的无障碍名称，VoiceOver 不再朗读 SF Symbol 内部标识。

- 浅色模式的标题栏、编辑器、Markdown 预览和底部状态栏统一使用 `#FBFBFB`，
  消除窗口各区域之间轻微但明显的底色差异；深色模式继续沿用原有深色背景。
- 侧边栏改为带轻微玻璃透视感的系统材质，并使用从窗口顶部贯穿到底部的右侧分隔线；
  移除“最近文件”标题下方多余的横线，使侧边栏更接近原生 macOS 抽屉层级。
- 标签末尾拖放区域会自动覆盖最后一个标签右侧的全部剩余空间，并保留稳定的最小落点，
  将标签拖到最右侧时无需再精确对准狭窄热区。
- 标签栏改为 macOS 26 Finder 风格的连续半透明轨道：激活标签使用轻薄胶囊背景，
  未激活标签通过短分隔线和悬停反馈区分，并将新建按钮调整为独立圆形按钮。
- 只有一个标签时自动隐藏标签栏；新建第二个标签或关闭回单标签时，编辑区会随标签栏
  平滑展开或收起。
- 标签宽度会根据窗口空间在稳定的最小宽度与合理最大宽度之间弹性调整；标签较多时继续
  支持滚轮横向浏览和边缘渐隐提示。
- 侧边栏会记住上次的展开或收起状态，重新启动应用及新建窗口时保持一致。
- 统一菜单命令的 SF Symbols 图标与文字缩进，使文件、编辑、显示和标签页菜单对齐一致。

### 修复

- 修复切换到新标签后立即输入时，旧编辑器仍可能短暂保留键盘焦点、导致内容同时写入原标签的
  问题；编辑器脱离窗口时现在会主动交还 first responder，新视图挂载时同步接管焦点。
- 修复 Markdown 行内代码中的加粗、斜体或链接标记仍会被预览二次解析的问题；代码内容
  现在始终按原文显示。
- 修复正文恰好包含内部行内代码占位文本时可能被预览误替换的极端情况。
- 修复 Markdown 链接地址包含下划线时，预览可能把地址片段错误解析为斜体并破坏链接的问题。
- 修复 JSON 光标位于已闭合嵌套对象之后时，折叠命令可能误选前一个对象的问题。
- 修复部分无效 JSON 只显示系统泛化错误的问题；现在会读取解析器诊断并在状态栏显示
  更具体的行号和列号。
- 修复旧式 `CR` 换行或 Unicode 行分隔符文本中的行号、光标行列和总行数计算错误；
  `LF`、`CRLF`、`CR`、U+2028、U+2029 现在均按一个逻辑换行处理。
- 修复 CRLF Markdown 可能被预览当作两个换行、产生多余段落，以及特殊换行结尾未显示
  最后一行行号的问题。

- “查找与替换”改为独立的非模态窗口，不再作为编辑窗口的附属面板；查找窗口打开后
  仍可自由点击、选中和编辑正文，`Command+W` 会关闭查找窗口而不会误关编辑标签；
  关闭主窗口时同步关闭对应的查找窗口。
- 修复 macOS 26 下侧边栏按钮和窗口标题出现大块圆角共享背景的问题，并让标题继续随
  侧边栏状态移动。
- 修复撤销、重做和语言模式工具被挤在标题旁边的问题；顶部操作组现在稳定固定在窗口右侧。
- 统一窗口标题栏背景与分隔线表现，避免侧边栏区域和编辑区域出现白色、灰色两种底色。
- 修复侧边栏展开后窗口标题落在侧边栏与编辑区交界处的问题；标题现在会随侧边栏状态
  对齐编辑区域上方。
- 修复展开或收起侧边栏时，行号标尺局部刷新可能残留旧数字，导致第一行偶尔显示为
  “41”等错误号码的问题。
- 修复标签无法拖到当前最后一个标签之后的问题；标签左右半区现在分别对应前插和后插，
  并在实际插入边缘显示位置指示线。
- 修复 JavaScript 和 TypeScript 正则字面量中的引号、转义斜杠被误判为字符串或注释，
  进而导致后续多行代码持续使用错误高亮颜色的问题。
- 修复标签数量超出窗口宽度后，标签栏未能绑定横向滚动视图，导致鼠标滚轮无法左右浏览的问题。
- 修复代码行注释在回车符或 Unicode 换行符后没有结束，以及未闭合的单行字符串可能让后续代码
  持续使用错误高亮颜色的问题。
- 修复通过 `Command+N` 新建的窗口同时显示系统标题和自定义标题、造成标题重复的问题。
- 修复从最近文件重命名已在多个窗口打开的文件时，只有当前窗口更新路径的问题；所有窗口中的
  对应标签、语言模式和预览状态现在会同步更新。
- 修复普通文本另存为 Markdown 后预览没有自动开启，以及 Markdown 另存为其他格式时预览状态
  没有同步清理的问题。
- 修复 Markdown 标题和 JSON 容器折叠后只隐藏文字、仍保留大块空白高度的问题；折叠区
  现在会真正收缩布局，展开恢复原高度，编辑或查找定位到折叠内容时会安全展开。
- 修复大文件在光标滚离视口后输入时，光标、视口、状态栏与行号可能不同步的问题；普通
  输入和中文输入法提交现在都会返回真实编辑位置，并清理标尺边缘残留的旧行号。
- 修复系统恢复出多个主窗口时可能错误共享同一编辑状态的问题；主窗口现在保持唯一，
  `Command+N` 和标签分离创建的窗口继续拥有各自独立的标签与文档状态。

## [0.3.0] - 2026-07-31

本版本重构代码高亮与标签拖放能力，并针对大文件编辑、标签切换和行列定位进行性能优化。

### 新增

- 标签拖放改用原生 macOS 拖动会话，拖动时显示跟随指针的标签预览和插入位置。
- 支持将标签直接拖入另一个 LacEditor 窗口；落在标签栏时按指定位置插入，落在窗口其他
  区域时追加到该窗口。
- 清空最近文件前增加确认提示，明确说明只会移除历史记录，不会删除磁盘文件。

### 优化

- 重构 Markdown、JSON、HTML、JavaScript、TypeScript、CSS、Python、Swift、Shell、
  YAML、C/C++ 和 SQL 的语法高亮规则，使用适配浅色与深色外观的克制配色。
- 语法高亮会先识别字符串和注释，再处理关键字、数字和其他语义元素，避免同一段内容被
  后续规则错误覆盖。
- 缓存已编译的高亮规则，减少输入过程中反复创建正则表达式的开销。
- 编辑器与行号仅布局当前可视区域，不再为了光标或滚动更新强制布局完整文档。
- 使用增量行索引计算行号和光标位置，插入或删除换行后无需从文档开头重新扫描。
- 最多保留最近访问的 8 个原生编辑器实例，在控制内存占用的同时加快常用标签切换。
- 标签拖动期间不再持续修改 SwiftUI 布局，减少拖动抖动，并在系统未回调标准落点时根据
  最后命中的标签可靠完成排序。

### 修复

- 修复字符串中的 `//`、`#`、`--` 等字符被错误识别为注释的问题。
- 修复关键字、数字和布尔值在字符串或注释内部仍被着色的问题。
- 修复 Markdown 行内代码和代码块中的强调符号仍会触发加粗或斜体高亮的问题。
- 修复 JSON 对象键与普通字符串无法区分，以及 HTML 注释中的标签仍被高亮的问题。
- 修复深色模式下当前行背景过暗、光标所在行不易辨认的问题。
- 修复标签拖出窗口后可能卡住，以及跨窗口拖放无法稳定合并或同窗口排序未提交的问题。

## [0.2.0] - 2026-07-30

本版本补全多窗口与标签管理体验，扩展常用代码格式，并集中改善侧边栏、菜单、Markdown
列表及大文件编辑性能。

### 新增

- 支持使用 `Command+1` 至 `Command+9` 快速切换到对应位置的标签页。
- “显示”菜单新增行号和底部状态栏的独立显示开关，并记住用户选择。
- 最近文件右键菜单支持直接重命名文件，已打开标签和最近文件路径会同步更新。
- 支持多窗口编辑，`Command+N` 新建独立窗口，每个窗口拥有自己的标签集合。
- 支持将标签拖出当前窗口，并在新的原生 macOS 窗口中继续编辑同一文档。
- 接入正式 LacEditor 应用图标，覆盖 Dock、Finder 和应用切换器所需尺寸。
- 工具栏新增撤销和重做按钮，同时保留系统菜单与标准快捷键。
- 支持使用 `Control+Tab` 切换到下一个标签，使用 `Control+Shift+Tab` 返回上一个标签。
- 标签支持直接拖动调整顺序。
- Markdown 有序列表按回车后会自动生成下一个序号，并支持 `1.` 和 `1)` 两种写法。
- Markdown 有序列表在中间插入或删除项目后，会自动整理同一连续序列的后续编号；
  直接修改编号仍视为手动设置。
- 新增 TypeScript、CSS、Python、Swift、Shell、YAML、C/C++ 和 SQL 文件识别与基础语法高亮。
- 最近文件支持右键用新窗口打开、在 Finder 中显示或从列表中单独删除。

### 优化

- 已访问标签会保留各自的原生编辑器实例，切换时直接显示已有内容；无变化时不再重复执行
  全文布局和语法高亮，缩短标签切换与文件首次显示等待。
- 大文件字数统计改为后台串行任务，并在继续输入时取消过期计算，避免重复扫描占用资源。
- 标签拖动改为轻量位移预览，松手后才提交一次顺序变化，减少连续拖动时的跳动和卡顿。
- 带快捷键的工具栏按钮会在悬停提示中同时显示对应按键组合。
- 优化标签新增、关闭、选中和拖动时的过渡动画，减少布局突变。
- 侧边栏改为 Codex 风格的左侧抽屉：固定展开时推动编辑区，收起后悬停工具栏按钮可临时覆盖显示。
- 标签关闭按钮默认隐藏，鼠标移入标签后显示；未激活标签悬停时提供轻微高亮反馈。
- 标签使用稳定的最小宽度；数量较多时可将鼠标移到标签栏，用滚轮直接横向浏览；
  两侧根据真实滚动位置显示更清晰的渐隐效果，切换标签后也会自动定位当前项。
- 左侧边栏改为只显示最近文件，并使用与主窗口一致的背景、紧凑行高和清晰文件路径层级。
- 查找与替换不再持续占用空间展示全部转义字符说明；将鼠标停留在问号图标上即可查看。

### 修复

- 修复“显示 > 外观”子菜单无法通过鼠标悬停展开的问题。
- 修复标签拖出窗口时，窗口创建与拖动事务互相阻塞导致整个应用卡住的问题。
- 修复粘贴带显式编号的 Markdown 有序列表后，在末尾按回车会重写前面编号的问题。
- 修复从深色外观切换为“跟随系统”后，浅色界面仍残留浅色文字的问题。
- 修复手动创建的新窗口没有显示侧边栏收起与展开按钮的问题。
- 修复行号标尺边缘可能覆盖编辑区左侧文字的问题，并增加正文安全内边距。
- 修复唯一有内容标签关闭后的空白标签无法继续通过 `Command+W` 关闭窗口的问题。
- 修复开启文本换行后，按住 `Shift` 滚动滚轮仍能让编辑区域产生水平位移的问题。
- 修复图标按钮的系统 Tooltip 在鼠标停留后仍一闪而过的问题，改为延迟出现并保持到鼠标移开。
- 修复开启文本换行和 Markdown 预览后，编辑器最右侧文字仍可能被预览区域遮挡的问题。
- 修复新建文档输入内容后再全部删除，关闭时仍提示保存的问题。

## [0.1.0] - 2026-07-30

LacEditor 的首个可运行预览版本，完成轻量文本与代码编辑器的第一阶段能力。

### 新增

- 支持新建、打开、编辑、保存、另存为和关闭文本文件，并提供拖拽打开与最近文件列表。
- 支持 TXT、Markdown、HTML、JSON 和 JavaScript 文件；JavaScript 同时识别 `.js`、`.mjs` 和 `.cjs`。
- 支持 UTF-8 默认编码；遇到非 UTF-8 文件时，可选择 UTF-16、GB 18030、ISO Latin 1、
  Windows Latin 1 或 Mac OS Roman 编码打开。
- 提供顶部多标签栏，支持切换标签、关闭当前标签、关闭其他标签和关闭右侧标签，并避免重复打开同一文件。
- 提供未保存标记；关闭标签、窗口或退出应用前，会确认是否保存更改。
- 编辑器支持行号、当前行高亮、自动缩进、Tab 缩进、撤销、重做、自动换行和字体缩放。
- 提供统一的“查找与替换”弹窗，支持查找上一个/下一个、单次替换、全部替换和区分大小写。
- 查找与替换支持 `\n`、`\r`、`\t`、`\s`、`\\` 等转义写法，可搜索换行、回车、
  制表符、空格和反斜杠。
- 为 Markdown、JSON、HTML 和 JavaScript 提供低干扰语法高亮。
- Markdown 提供左右可调分栏的实时预览，支持标题、段落、列表、引用、分割线、链接、
  行内代码、代码块和表格。
- JSON 提供格式化和压缩命令，并在内容无效时尽量报告错误行号和列号。
- 提供可收起文件侧边栏、文件树、最近文件、紧凑状态栏和语言模式菜单。
- 支持跟随系统、浅色和深色三种外观模式，并提供原生 macOS 菜单和快捷键。
- Markdown 标题和 JSON 容器支持基础代码折叠。

### 优化

- 编辑器基于原生 `NSTextView`，大文件仅高亮可视区域及缓冲区，以保持输入流畅。
- Markdown 预览采用轻微防抖，并通过内容版本校验确保快速输入时展示最新结果。
- 分栏宽度变化后会重新计算编辑器文本容器，长行始终限制在当前编辑区域内。
- 右上角语言模式菜单同时显示文件类型图标和语言名称，减少识别成本。
- 重新设计设置窗口的排版和尺寸，使外观、自动换行与字体设置完整可见。

### 修复

- 修复滚动、长文本和自动换行场景下行号与逻辑文本行错位的问题。
- 修复 Markdown 有序列表被嵌套无序列表打断后，从 1 重新编号的问题。
- 修复快速连续编辑时 Markdown 预览可能停留在旧内容的问题。
- 修复 Markdown 预览开启后，编辑器长文本延伸到预览区域下方的问题。
- 修复设置窗口高度不足导致部分内容被裁切的问题。
